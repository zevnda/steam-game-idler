export function windowMinimize(appWindow) {
    appWindow?.minimize();
}

export function windowToggleMaximize(appWindow) {
    appWindow?.toggleMaximize();
}

export function windowClose(appWindow) {
    appWindow?.close();
}
