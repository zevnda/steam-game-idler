---
description: "The FAQ section provides answers to common questions and issues about using SGI."
---

import CopyableFAQ from '../../components/CopyableFAQ';
import { faqData } from './faqData';
import ReactMarkdown from 'react-markdown';

# FAQ

{faqData.map((faq, i) =>
  'section' in faq ? (
    <h2 key={faq.section + i} className='x:tracking-tight x:text-slate-900 x:dark:text-slate-100 x:font-semibold x:target:animate-[fade-in_1.5s] x:mt-10 x:border-b x:pb-1 x:text-3xl nextra-border'>
    {faq.section}
    </h2>
  ) : (
    <CopyableFAQ key={faq.question} id={faq.question} question={faq.question}>
      <ReactMarkdown>{faq.markdown.split('\n').slice(1).join('\n')}</ReactMarkdown>
    </CopyableFAQ>
  )
)}