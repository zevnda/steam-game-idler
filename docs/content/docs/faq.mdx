---
title: Frequently Asked Questions
description: Commonly asked questions and answers about features and usage.
---

import CopyableFAQ from '@docs/components/content/CopyableFAQ';
import { faqData } from './faqData';
import ReactMarkdown from 'react-markdown';
import { Accordions } from 'fumadocs-ui/components/accordion';

# Frequently Asked Questions

{(() => {
  const sections = [];
  let currentFaqs = [];
  let currentSection = null;
  faqData.forEach((faq, i) => {
    if ('section' in faq) {
      if (currentFaqs.length > 0) {
        sections.push(
          <Accordions type="multiple" key={'faqs-' + currentSection}>
            {currentFaqs}
          </Accordions>
        );
        currentFaqs = [];
      }
      sections.push(<h2 key={faq.section + i}>{faq.section}</h2>);
      currentSection = faq.section;
    } else {
      currentFaqs.push(
        <CopyableFAQ
          key={faq.question}
          id={faq.question}
          question={faq.question}
          value={faq.question}
        >
          <ReactMarkdown>{faq.markdown.split('\n').slice(1).join('\n')}</ReactMarkdown>
        </CopyableFAQ>
      );
    }
  });
  if (currentFaqs.length > 0) {
    sections.push(
      <Accordions type="multiple" key={'faqs-last'}>
        {currentFaqs}
      </Accordions>
    );
  }
  return sections;
})()}